# Copyright (c) Meta Platforms, Inc. and affiliates.

name: "RFC: Component or Feature Proposal"
description: Propose a new component, feature, or significant API change for Khameleon.
title: "[RFC] "
labels: ["enhancement"]
body:
  - type: markdown
    attributes:
      value: |
        ## Before you submit

        An RFC is a **problem statement and demand signal** — not a design proposal we evaluate as-written. Read the [contributing guide](https://github.com/the404-nf/khameleon/wiki/Contributing) before submitting.

        After acceptance, either of us drives the [specification protocol](https://github.com/the404-nf/khameleon/wiki/Component-Specification-Protocol) — you can front-load research and API exploration in this RFC at your own risk if we haven't responded yet.

        **Response time:** We aim to respond within 1 week — either accepting, requesting more information, or declining with rationale.

  - type: textarea
    id: problem
    attributes:
      label: Problem Statement
      description: What use case are you solving? Describe the user experience gap, not the component you want.
      placeholder: "Users need to [do X] and currently can't because [Y]. This shows up when building [Z]."
    validations:
      required: true

  - type: textarea
    id: evidence
    attributes:
      label: Evidence of Demand
      description: |
        Where does this pattern appear? The more concrete, the better.
        - Screenshots or wireframes from distinct products/surfaces
        - Links to external systems that ship this (Radix, Chakra, MUI, Ant Design)
        - Frequency — how often does this pattern come up?
      placeholder: "This pattern appears in [A], [B], and [C]. Here are examples: ..."
    validations:
      required: true

  - type: textarea
    id: existing
    attributes:
      label: Why Existing Components Don't Cover This
      description: |
        What have you tried with current Khameleon components? Show the composition attempt and where it breaks down. "I tried X but it requires Y lines of boilerplate / breaks accessibility / doesn't theme correctly."
      placeholder: "I attempted to compose this using Card + VStack + ... but ..."
    validations:
      required: true

  - type: textarea
    id: approaches
    attributes:
      label: Rough Approaches Considered
      description: |
        One or more ideas for how this could work — not as implementation proposals, but to show you've thought through the design space. Include consumer code examples if possible.
      placeholder: |
        Option A: A new Foo component that...
        ```tsx
        <Foo prop={value}>...</Foo>
        ```

        Option B: A hook that...
        ```tsx
        const fooProps = useFoo({...});
        <div {...fooProps}>...</div>
        ```
    validations:
      required: false

  - type: textarea
    id: accessibility
    attributes:
      label: Accessibility Considerations
      description: What ARIA pattern does this map to? What keyboard interactions are expected? Who benefits?
      placeholder: "This maps to the ARIA [pattern] role. Users should be able to [keyboard interaction]."
    validations:
      required: true

  - type: textarea
    id: performance
    attributes:
      label: Performance Considerations
      description: Only required for components that render multiple items (lists, tables, grids). Address DOM node counts, virtualization, and measurement strategy.
      placeholder: "For N items, this renders [X] DOM nodes. Virtualization strategy: ..."
    validations:
      required: false

  - type: checkboxes
    id: checklist
    attributes:
      label: Pre-submission Checklist
      options:
        - label: I have read the [Contributing guide](https://github.com/the404-nf/khameleon/wiki/Contributing)
          required: true
        - label: I have read the [API Conventions](https://github.com/the404-nf/khameleon/wiki/API-Conventions)
          required: true
        - label: I have checked that existing Khameleon components cannot compose to solve this
          required: true
        - label: This is a general-purpose UI pattern (not specific to one product)
          required: true
